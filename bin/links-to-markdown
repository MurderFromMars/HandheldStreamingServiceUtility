#!/bin/sh

# Reformats entries from data/links.index into Markdown-style link bullets.
# Original format:  Name|URL|Browser
# Output format:    * [Name](URL)

while IFS='|' read -r NAME URL _BROWSER
do
    printf '* [%s](%s)\n' "$NAME" "$URL"
done < data/links.index
